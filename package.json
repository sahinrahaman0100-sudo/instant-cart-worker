{
  "name": "store-backend-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute store_db --file schema.sql",
    "db:seed": "wrangler d1 execute store_db --file seed.sql"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260410.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.11.1"
  }
}
